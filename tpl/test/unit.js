"use strict";

var assert = require('assert'),
    fs = require('fs'),
    config = require('../config/' + (process.env.NODE_ENV || 'development'),
    PROJECT_NAME_CAPS = require('../lib/PROJECT_NAME');


describe('PROJECT_NAME_CAPS', function(){
    before(function(cb){
        cb();
    });

    beforeEach(function(cb){
        cb();
    });

    afterEach(function(cb){
        cb();
    });

    after(function(cb){
        cb();
    });

});
